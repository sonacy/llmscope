{
  "name": "llmscope",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/daemon",
    "packages/cli",
    "packages/ui",
    "packages/whistle-llmscope"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun test",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "eslint .",
    "format": "prettier --write .",
    "dev": "bun run --filter '*' dev"
  },
  "packageManager": "bun@1.3.10",
  "engines": {
    "bun": ">=1.1.0"
  },
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.13.0",
    "@types/bun": "latest",
    "eslint": "^9.13.0",
    "globals": "^15.11.0",
    "prettier": "^3.3.0",
    "typescript": "^5.6.0"
  }
}
